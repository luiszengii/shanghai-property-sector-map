# Use record revisions and ledger snapshots

Sources, evidence records, and price snapshots will each retain immutable revisions, while an explicit ledger version freezes the selected revisions as one recoverable state. Project facts and price observations evolve independently, unlike a purely coupled boundary dataset; record-level history explains a single change, and the ledger snapshot reproduces a public projection or a complete rollback.
